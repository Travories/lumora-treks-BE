version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: lumora_redis
    restart: always
    ports:
      - "6381:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel notice

volumes:
  redis_data:
